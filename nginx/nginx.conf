user  root;
worker_processes 1;

events {
}

http {
    server {
        listen 80;
        # server_name   your_server;

        location / {
            proxy_pass http://frontend:4000/;
        }

        location /api/ {
            proxy_pass http://backend:8000/;
        }
    }
}
